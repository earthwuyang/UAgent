# LLM provider API keys (set at least one)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
OPENROUTER_API_KEY=

# LiteLLM defaults (optional overrides)
LITELLM_API_KEY=${OPENAI_API_KEY}
LITELLM_MODEL=openai/glm-4.5
LITELLM_API_BASE=
LITELLM_EXTRA_OPTIONS=
LITELLM_CLASSIFICATION_MAX_TOKENS=64000

# Backend server
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8001
BACKEND_RELOAD=true
UAGENT_WORKSPACE_DIR=/home/wuy/AI/uagent-workspace

# OpenHands runtime / Docker integration
SANDBOX_VOLUMES=
SANDBOX_USER_ID=
# Optional Linux networking assists (use only if runtime cannot connect)
# SANDBOX_USE_HOST_NETWORK=true
# DOCKER_HOST_ADDR=127.0.0.1

# Frontend
VITE_DEV_SERVER_PORT=3000
VITE_BACKEND_URL=http://127.0.0.1:${BACKEND_PORT}
